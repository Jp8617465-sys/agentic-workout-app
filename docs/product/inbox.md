# James inbox

Decisions only James can settle. arbi surfaces every OPEN row in the brief's
DECISIONS NEEDED section and never treats one as resolved until James rules.

| opened | decision needed | what it gates | options | status |
|---|---|---|---|---|
| 2026-08-16 | Enable branch protection on `main` with required status checks (`check`) and code-owner review | Everything. The I6a auto-merge grant is conditioned on "required checks green" — without protection the agent can merge red and nothing stops it, and `CODEOWNERS` is inert so the governance surface is not actually protected from self-modification | (a) enable both settings — recommended; (b) enable required checks only; (c) leave off and accept that I6a is prompt-enforced | **OPEN** |
| 2026-08-16 | The four boundary docs (`north-star.md`, `arbi-constitution.md`, `arbi-authority.md`, `arbi-scorecard.md`) and the CLAUDE.md arbi routing line were not written before `authority-guard` was registered, and that hook now denies those exact paths | arbi's tie-breaking (needs north-star §1.3), its self-scoring, and the main loop routing to arbi instead of freelancing | **Correction to the original options here:** unregistering the hook alone is not enough — `.claude/settings.json`'s native `deny` array blocks these paths independently, and that file denies edits to itself. Draft content for all four is ready (delivered 2026-08-16, see chat/session record). Real unblock paths: (a) James applies the drafts directly via GitHub's UI or local git — no session tooling involved, recommended for CLAUDE.md and the three governance docs; (b) James removes the matching four entries from `.claude/settings.json`'s deny array himself, then the main loop drafts a PR — still gated by CODEOWNERS + his review before merge, since removing the settings.json entry doesn't remove CODEOWNERS; (c) leave north-star.md unwritten regardless — that one is James's to author, not to unblock for the agent | **OPEN — drafts ready, awaiting James** |
| 2026-08-16 | Write `north-star.md` §1.1 The Output and §1.2 Non-negotiables | arbi reads this every wake and never authors it. Until it exists arbi has no boundary to check recommendations against and no defensibility ladder to break ties with | Only James can write this one — it is the definition of done for the whole project | **OPEN** |
| 2026-08-16 | ~~Remove or repoint the Supabase MCP in `.mcp.json`~~ | It targeted project `lerhdwwihmiuyqmyxrrh` — an ASX stock-portfolio schema (749,600 price rows, `theses`, `holding_lots`) with RLS disabled on 51 tables, containing no workout tables | Removed, along with the matching URL in `.env.example`, per James's local-only decision | **RESOLVED 2026-08-16** |
| 2026-08-16 | The rest timer is orphaned: `RestTimerCompact`, `RestTimerFullScreen` and `useRestTimer` are imported by nothing, though ROADMAP marks the feature ✅ and it is a headline item in the brief | Whether a documented core feature is real | (a) wire it into the active workout screen; (b) delete it and correct ROADMAP | **OPEN** |
| 2026-08-16 | Apply the `push-guard.sh` branch-mismatch fix (`.git/.../push-guard-branch-mismatch-fix.patch`, handed to James this session) — same blocker shape as row 2: `.claude/hooks/**` is denied at the native settings.json level with no branch-awareness, so this session cannot edit it on any branch | Closing the exact mechanism that let Mission 1's commit land via a raw push instead of a reviewed merge (risk-register.md #10). Not blocking further missions — the safeguard is process discipline in the meantime, not a hard gate | James applies the patch directly via GitHub or local git | **OPEN** |
| 2026-08-16 | Is the sync engine worth completing, given the local-only decision? | ~1,100 lines (438-line stub + 666 lines of orphaned adapters) and 1,396 lines of docs describing it as working | (a) implement `createSyncJobs()` from the adapters; (b) gate the UI off and keep the code for later; (c) delete both and the docs | **OPEN** |
